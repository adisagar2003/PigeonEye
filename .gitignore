.env
.venv/
__pycache__/

# compiled swift binaries — built to the root because eval/ and spikes/ shell
# out to ./ocr and ./spike_fm. Sources live in Sources/ and spikes/.
ocr
spike_fm
spike_form
.DS_Store

# golden eval datasets — 157 MB, refetch with assets/golden/fetch.sh
assets/golden/*
!assets/golden/README.md
!assets/golden/fetch.sh
!assets/golden/verify.py
.build/
