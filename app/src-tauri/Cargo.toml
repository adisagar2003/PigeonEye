[package]
name = "pigeoneye"
version = "0.1.0"
edition = "2021"
description = "PigeonEye — reads government documents on your machine"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[dependencies]
tauri = { version = "2.11", features = [] }
